{
  "name": "npd-bot",
  "version": "0.1.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.ts",
    "clean": "rimraf dist/",
    "copy-files": "copyfiles assets/**/* creds/**/* dist/",
    "copy-web": "copyfiles -u 1 ../build/**/* dist/",
    "build": "cd .. && npm run react-build && cd server && npm run clean && tsc && npm run copy-files && npm run copy-web",
    "client-build": "npm run clean && npm i && tsc && npm run copy-files && npm run copy-web",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "discord.js": "^13.8.1",
    "dotenv": "^16.0.1",
    "emailjs": "^3.7.0",
    "express": "^4.18.1",
    "googleapis": "^105.0.0",
    "mongodb": "^4.7.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "copyfiles": "^2.4.1",
    "nodemon": "^2.0.19",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  }
}
