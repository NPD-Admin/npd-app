
class NPDTestWidget {
  static init() {
    const widgetId = '<%= widgetDivId %>'
    console.log(<%= widgetDivId %>);
    console.log(widgetId);
    const widgetElement = document.getElementById(widgetId);
    console.log('!!!');
    console.log(widgetId);
    widgetElement.innerHTML = 'IT WORKS!';
  }
}

NPDTestWidget.init();