{
  "name": "npd-app",
  "version": "0.1.0",
  "proxy": "http://localhost:5001",
  "private": true,
  "dependencies": {
    "@react-oauth/google": "^0.2.6",
    "jwt-decode": "^3.1.2",
    "npd-app": "file:..",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "@types/jest": "^27.5.2",
    "@types/node": "^16.11.45",
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "react-build": "npm run clean && npm run react-build && npm run copy && cd server && npm run client-build",
    "clean": "rimraf server/dist/",
    "copy": "copyfiles build/**/* server/dist",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "prod": "cd server && npm run start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
